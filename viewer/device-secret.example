# Place your 32-byte pair secret at:
#   ~/.config/agent-switchboard/device-secret
# File mode 0600. Formats accepted: raw 32 bytes, hex (64 chars), or base64url.
# Also set device_id in ~/.config/agent-switchboard/edge.json (see edge.example.json).
# NEVER commit the real secret.
